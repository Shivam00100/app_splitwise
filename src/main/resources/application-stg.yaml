server:
  port: 8001

spring:
  application:
    name: lets-split

  datasource:
    url: jdbc:postgresql://localhost:5432/split_db?currentSchema=app_schema
    username: app_user
    password: app_user123
    driver-class-name: org.postgresql.Driver

  sql:
    init:
      mode: always # mark it never in prod
spring:
  application:
    name: splitwise

  datasource:
    url: jdbc:mysql://localhost:3306/splitwiseDB
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver

  sql:
    init:
      mode: always

server:
  port: 8084